services:
  mongodb:
    image: mongo:7
    container_name: interviewai-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - interviewai_mongodb_data:/data/db

volumes:
  interviewai_mongodb_data:
